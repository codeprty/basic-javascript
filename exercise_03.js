// Check if a number is positive
// Create a variable num = 10.
// Use an if statement to log "Positive" only if the number is greater than 0.
let num = 10;
if (num > 0) {
    console.log("Positive");
}

// Check if a user is adult
// Given age = 20, use an if statement to log "Adult" only if age is 18 or above.
let age = 20;
if (age >= 18) {
    console.log("Adult");
}

// Temperature check
// Given temp = 35, log "Hot day" only if temp is greater than or equal to 30.
let temp = 35;
if (temp >= 30) {
    console.log("Hot day");
}

// Check if a number is even
// Given x = 12, use if (x % 2 === 0) to log "Even number".
let x = 12;
if (x % 2 === 0) {
    console.log("Even number");
}

// Check if a string is not empty
// Given name = "John", log "Name provided" if the string length is greater than 0.
let name = "John";
if (name.length > 0) {
    console.log("Name provided");
}

// Check if score is perfect
// Given score = 100, log "Perfect Score!" only if score is exactly equal to 100.
let score = 100;
if (score === 100) {
    console.log("Perfect score!");
}

// Check login status
// Given let isLoggedIn = true, use if (isLoggedIn) to log "Welcome back!".
let isLoggedIn = true;
if (isLoggedIn) {
    console.log("Welcome back!");
}

// Check if discount applies
// Given let price = 250, use an if statement to log "Discount Applied" only if price is more than 200.
let price = 250;
if (price > 200) {
    console.log("Discount Applied");
}

// Check if a number is within range
// Given let n = 15, log "In range" only if n is greater than 10 AND less than 20.
let n = 15;
if (n > 10 && n < 20) {
    console.log("In range");
}

// Check if a password is long enough
// Given let password = "abcd1234", log "Password length OK" only if the password length is 8 or more characters.
let password = "abcd1234";
if (password.length >= 8) {
    console.log("Password length OK");
}
