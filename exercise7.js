// Positive or Negative
// Given num = 5, use a ternary operator to log "Positive" or "Negative".
let num = 5;
num = (num > 0) ? "Positive" : "Negative";
console.log(num);

// Adult or Minor
// Given age = 17, output "Adult" if age ≥ 18, otherwise "Minor".
let age = 17;
age = (age >= 18) ? "Adult" : "Minor";
console.log(age);

// Even or Odd
// Given x = 8, use a ternary to print "Even" or "Odd".
let x = 8;
x = (x % 2 === 0) ? 'Even' : "Odd";
console.log(x);

// Login Status
// Given isLoggedIn = true, print "Welcome" if true, otherwise "Please login".
let isLoggedIn = true;
isLoggedIn = (isLoggedIn) ? "Welcome" : "Please login";
console.log(isLoggedIn);

// Temperature Check
// Given temp = 28, print "Hot" if temp ≥ 30, otherwise "Normal".
let temp = 28;
temp = (temp >= 30) ? "Hot" : "Normal";
console.log(temp);

// Compare Two Numbers
// Given a = 10 and b = 20, print "a is larger" if a > b, otherwise "b is larger".
let a = 10, b = 20;
a = (a > b) ? "a is larger" : "b is larger";
console.log(a);

// Discount Eligibility
// Given price = 150, print "Discount" if price ≥ 100, otherwise "No discount".
let price = 150;
price = (price >= 100) ? "Discount" : "No discount";
console.log(price);

// Password Length Check
// Given password = "abc123", print "Valid" if password length ≥ 6, otherwise "Invalid".
let password = "abc123";
password = (password.length >= 6) ? "Valid" : "Invalid";
console.log(password);

// Score Result
// Given score = 45, print "Pass" if score ≥ 50, otherwise "Fail".
let score = 45;
score = (score >= 50) ? "Pass" : "Fail";
console.log(score);

// Dark Mode Toggle
// Given darkMode = false, print "Dark mode ON" if true, otherwise "Dark mode OFF".
let darkMode = false;
darkMode = darkMode ? "Dark mode ON" : "Dark mode OFF";
console.log(darkMode);
