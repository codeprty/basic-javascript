// Print "Hello, World!" to the console.
console.log("Hello, World!");

// Store your name in a variable and print "Hello, [your name]".
const yourName = "Anri Okita";
console.log(`Hello, ${yourName}`);

// Convert centimeters to inches
// Formula: inches = cm × 0.394
const cm = 60;
const inch = cm * 0.394;
console.log(`${cm} cm is equal to ${inch} inches`);

// String concatenation
// Store your first name and last name in two variables, then print your full name.
const firstName = "Anri";
const lastName = "Okita";
console.log(`Full Name: ${firstName} ${lastName}`);

// String length
// Store a sentence in a variable and print how many characters it has.
const sentence = "A dream within a dream"
console.log(`String length: ${sentence.length}`);

// Uppercase conversion
// Store your favorite food in a variable, then print it in all uppercase letters.
const food = "char kueh tiaw";
console.log(food.toUpperCase());

// Area of a rectangle
// Store the width and height, then print the area.
const width = 50;
const height = 20;
const area = width * height;
console.log(`Area: ${area}`);

// Store two numbers in variables
// Print out their sum, difference, product, quotient, and remainder
const num1 = 5;
const num2 = 2;

const sum = num1 + num2;
const difference = num1 - num2;
const product = num1 * num2;
const quotient = num1 / num2;
const remainder = num1 % num2;

console.log(`Sum: ${sum}`);
console.log(`Difference: ${difference}`);
console.log(`Product: ${product}`);
console.log(`Quotient: ${quotient}`);
console.log(`Remainder: ${remainder}`);